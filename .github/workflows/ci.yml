name: PR Checks

on:
  pull_request:

permissions:
  statuses: write
  checks: write
  contents: read
  pull-requests: read
  security-events: write

jobs:
  pr-checks:
    uses: actionsforge/actions/.github/workflows/python-image-pr-checks.yml@main
    with:
      python-version: "3.13"
      test-command: pytest tests/ -v -m "not integration_geonet"
      trivy-vuln-type: library
