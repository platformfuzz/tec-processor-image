# requirements.lock — exact resolved versions for reproducible container builds.
# This file records pinned (==) versions of all direct and transitive dependencies
# installed in the tec-processor-image container. It is consumed by the Dockerfile
# via `pip install -r requirements.lock` to ensure identical installs across builds.
#
# Direct dependencies: pytecgg, pyarrow, polars, boto3, awslambdaric, matplotlib, plotly
# Transitive (via pytecgg/boto3): numpy, scipy, numba, llvmlite, pymap3d, tzdata,
#   ppigrf, requests, pandas, certifi, charset-normalizer, idna
# Transitive (via matplotlib): contourpy, cycler, fonttools, kiwisolver
# Transitive (via plotly): narwhals
#
# Regenerate this file when upgrading dependencies by resolving the full dependency
# tree from pyproject.toml constraints on Python 3.13.

# --- Direct dependencies ---
pytecgg==1.3.0
pyarrow==23.0.1
polars==1.18.0
boto3==1.43.36
awslambdaric==4.0.1
matplotlib==3.11.0
plotly==6.8.0

# --- Transitive dependencies (via matplotlib) ---
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0

# --- Transitive dependencies (via plotly) ---
narwhals==2.22.1

# --- Transitive dependencies (via pytecgg/boto3) ---
botocore==1.43.36
jmespath==1.1.0
s3transfer==0.19.0
numpy==2.4.6
scipy==1.18.0
numba==0.65.1
llvmlite==0.47.0
pymap3d==3.2.0
tzdata==2026.2
ppigrf==2.1.0
pandas==3.0.3
requests==2.34.2
certifi==2026.6.17
charset-normalizer==3.4.7
idna==3.18
